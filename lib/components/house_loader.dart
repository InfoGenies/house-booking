


import 'package:flutter/material.dart';

class HouseLoader extends StatelessWidget {
  const HouseLoader({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
